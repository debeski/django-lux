# Project Tracker (django-lux)

## Part 1: Project Related
### Current Verified Snapshot:
- v1.2.14 TAGGED + RELEASED. Working tree = ONE unreleased v1.3.0 (per user decision: the never-tagged v1.2.15 was FOLDED into v1.3.0 — its CHANGELOG bullets merged up under `## v1.3.0`, no standalone v1.2.15 section). v1.3.0 = Permission Groups/Presets + folded former-1.2.15 frontend polish (Options landing/public-root, System Info diagnostics) + dark-theme contrast fixes + activity-log tab-count fix. Manifest = v1.3.0, INLINE_SAFE. Tag once as v1.3.0.
- `dlux/release-manifest.json` is the version source (now v1.3.0, inline_safe:TRUE). Migration `0007` (`GroupProfile`+`GroupMembership`) is inline-safe = two additive CreateModels ONLY (no AlterModelOptions) → passes `release_check.validate_inline_migrations` (allowlist CreateModel/AddField/AddIndex). KEY: `dlux.manage_groups` perm is declared on the NEW `GroupProfile.Meta` (not existing Profile) precisely so it rides in CreateModel.options; codename still resolves `dlux.manage_groups`. Prior baseline `0006` (v1.2.10). Built on native `auth.Group` (has_perm unions group perms → non-breaking).
- v1.2.7 remains the mandatory rebuilt baseline before later manifest-approved inline releases.
- DjangoLux supplies settings/setup, scoped models, auth/security, navigation, reports, backup, scaffolding, SSO hooks, and the Compose updater.
- Updater state uses `DluxUpdateState`/`DluxUpdateRun` plus `dlux_runtime` releases, atomic pointer, generation, maintenance, heartbeat, and degraded markers.
- `switch_pos` is rolled back to active/baked v1.2.4 but degraded with maintenance retained after candidate and rollback Celery probes raced normal startup. v1.2.13 reconcile now SELF-HEALS this class: once back on baked it clears the stuck degraded flag (needs one reconcile post-upgrade, or a one-time flag clear / `down -v` since the current row predates the fix).

### Current Project Adopted Standards:
- Integrate settings with `from dlux.utils import dlux_settings`; call `dlux_settings(globals())`; mount `dlux.urls` at root.
- `dlux.system` owns settings defaults/schema/normalizers/registry; migration-history wrappers remain in `dlux.models`.
- DSRP-1 requires backend authorization to match UI visibility and security mutations to be POST-only.
- Authored runtime CSS/data/events/JS use the `dlux` prefix and external assets; user copy uses `DLUX_STRINGS`.
- Releases are tag-driven; package/version/release validation all read `dlux/release-manifest.json`.

### Adopted Standards' rules and policies:
- Maintain this tracker as verified project state, <=100 lines total; Part 1 <=55 lines.
- Root changelog sections are newest-first `## vX.Y.Z` with flat bold-title bullets; tagged sections are immutable.
- Never delete repository files; relocate obsolete/replaced material under `.xpose/` with relative-path preservation.
- Preserve unrelated user changes and use `apply_patch` for source/doc edits.
- NEVER put raw HTML tags in i18n strings or form label/help_text (en+ar+fallbacks): crispy renders help_text UNescaped, and an injected tag (e.g. `<title>`) silently truncates innerHTML-loaded dynamic-modal forms — swallowing every node after it with NO Python/JS error. Use plain words or HTML entities.
- ALWAYS reuse dlux-provided components, never roll your own: settings-form choice fields MUST bind `DluxChoiceSelectorWidget` via `_bind_choice_selector_widget` (variant `toggle` for few options, `card`+searchable for many) — never plain `forms.Select`; toggles use `build_settings_toggle_field`; modals use the dynamic modal; spinners use `DluxLoadingButton`; strings via `DLUX_STRINGS`/`s.get`. New settings keys follow the existing field's full pattern (decl→schema→normalizer→default→form widget→initial→clean preservation→payload→APP_CONFIG→export/import→en+ar). All of this is DSRP-1 (CSP-safe data-*/external-asset controls) + form-consistency; match an existing field end-to-end before adding one.
- Feature/config/schema/API/security/deployment changes require same-turn technical documentation.

### Cross-Cutting Audits if any:
- 2026-06-23: Updater audit covered artifacts, attestation, dependency/migration gates, supervisor/runtime state, recovery, UI/API, scaffold, nginx, and Compose.
- 2026-06-24: ScanLink no PDF byte/page cap; gen-project nginx effective `5M` upload ceiling. Reports audit covered activity-window/Celery-backup/download-handoff/media/nginx.
- 2026-07-04: Scaffold-vs-`../project-decrees` audit — core dlux files IDENTICAL (entrypoint/gunicorn/nginx/smtp_relay/start.sh/gitattributes); scaffold is AHEAD (Dockerfile apt deps, supervisor `baked_version()`); decrees was gen'd on dlux 1.0.4/pinned 1.2.10 so most diffs = project-specific or project-lagging. Fixed: stray committed `.pyc` in scaffold_templates→`.xpose`. OPEN (need user call): back-port compose `post_start migrator` + smtp-relay `depends_on db/redis` from decrees? (healthcheck→`manage.py check` and web `build:.` look project-specific, NOT recommended).

### Current Project's Unsolved Known Bugs:
- Fallback file/download redirects remain a high-risk-deployment review point; `_safe_referer()` currently enforces allowed hosts.
- Mounted test Compose uses Redis sessions; never use live `cache.clear()` probes because they delete browser sessions.

### Incomplete Tasks:
- **Priority 1:**
  - [ ] Browser-validate setup Step 10 logging grid hydrate/serialize, audit tab, and prune after collectstatic.
  - [ ] Browser-validate v1.2.13: anon public root with show_sidebar_on_public on — confirm `sidebar_items.html` degrades gracefully (public/empty entries) for AnonymousUser.
- **Priority 2:**
  - [ ] Optional request-scoped `transaction.on_commit` activity aggregator; deferred due TestCase/order fragility.
- **Completed Recently:**
  - [x] v1.3.0 PERMISSION GROUPS/PRESETS (CHANGELOG; 663 tests green incl. new `test_groups.py`; migration 0007). Built on native `auth.Group`+new `GroupProfile`/`GroupMembership`; new `dlux.manage_groups` perm gates CRUD+membership. Preset selector in create/permissions forms (`_maybe_add_group_presets_field`, `DluxMultipleChoiceSelectorWidget`) layered over direct perms; `set_user_group_presets`/`set_group_members`/`get_visible_group_presets`/`can_manage_group_preset`/`get_manageable_users_queryset` in utils/users.py. Manage Groups modal (`views/groups.py`, `templates/dlux/groups/*`, `manage_groups.js` re-execs injected scripts; bespoke `#groupModal` like scopes). Scope-aware: global presets superuser/global-staff-managed, scoped staff own-scope only. Tier preview now unions group perms. INHERITANCE UX: permission widget shows preset-derived perms checked+disabled+"From group" badge (never written to direct user_permissions since disabled=not submitted); `permissions.js` recomputes live from a `group→perm-ids` json_script map on preset toggle, restoring admin's direct choices via `data-direct-checked`; `permission_inherited` en+ar; permissions.js/css buster→20260704a. `manage_groups` perm now has label `perm_manage_groups` + help `help_perm_manage_groups` (en+ar) + `DLUX_PERMISSION_HELP_TEXTS` entry, and is grouped into `staff_access` (Staff Tier & Access) with manage_staff/scopes in the widget. LAYOUT: permission model cards moved from Bootstrap `.row/.col-lg-4` (flex-row height coupling → lone short card hangs low) to CSS `.dlux-perm-columns` (column-count 3/2/1 + `break-inside:avoid`) so cards pack column-first; cards stay BLOCK (removed inline-block masonry hack that made multicol balancer under-fill → empty 3rd column); permissions.css buster→20260704c. Also AETHER table header `--dlux-table-header-surface` 135deg→180deg (per-th diagonal restarted per column = segmented look); theme buster→20260704a (2 test asserts updated). NOT browser-verified (no live server run).
  - [x] DARK-THEME CONTRAST + activity-log fixes (CHANGELOG; mostly CSS, inline-safe): (a) aether dynamic-modal text `:root.theme-aether #universalDynamicModalBody`→`var(--title)` (culprit: `dynamic_modal_detail.html` `.text-dark`); theme buster→`20260703a` (2 test asserts updated). (b) GROUPED-PERMISSION CARDS were hardcoded white + only darkened under `[data-bs-theme=dark]` (never set — theme-* system uses `:root.theme-X` classes) → white on ALL dark themes except dark.css. ROOT FIX in shared `permissions.css`: `.permissions-card/.model-group/-header/-title/.app-title/.dlux-staff-tier-preview` now read `--dlux-setup-card-bg/-item-bg/-item-border/-text/-muted` (all 7 dark themes define these globally; light themes use fallbacks) → correct on all 12 themes, NO per-theme rules. Removed the earlier aether-only patch. `permissions.css` buster→`20260703a`. (c) PASSWORD-RULES unmet → `var(--dlux-setup-muted, var(--bs-secondary-color,#6c757d))` (was fixed grey, dark-on-dark); buster→`20260703a`. (d) ACTIVITY-LOG tab badges stuck at 1 — `_base_queryset().order_by('-created_at')` leaked into `values('category').annotate(Count)` GROUP BY; added `.order_by()` (`views/activitylog.py`). (e) Manage Groups modal dropped Cancel btn (`_build_submit_only_actions`; Back button suffices). NOTE: CSS fixes need collectstatic+hard-reload in live/compose envs.
  - [x] v1.2.15 System Info diagnostics (CHANGELOG; `dlux/views/general.py`+`translations.py` only, no migrations, inline-safe): (a) CACHE row now appends Redis SERVER VERSION — new `_fetch_redis_version(cache_backend)` resolves the live client across django-redis (`client.get_client(write=False)`) + Django builtin RedisCache (`_cache.get_client()`), reads `INFO server`→`redis_version`; best-effort (falls back to bare `Redis` label). Only runs when `label=='Redis'`. (b) TASKS/Celery row is now LIVE — `_get_celery_service()` loads app via new `_get_celery_app()` (`<project>.celery.app` from `DJANGO_SETTINGS_MODULE`, fallback `celery.current_app`) then pings: Online+worker count (`service_celery_workers_online`), Offline on broker error/0 workers (`service_celery_no_workers`), Configured only if app unloadable (`service_celery_app_unavailable`). 3 new en+ar strings. LIGHTWEIGHT: worker ping blocks ~1s/broker round-trip (this is WHY live-check was dropped before) → new `_probe_celery_workers(app)` MEMOIZES the `(ok,count,error)` result in default cache (key `dlux:health:celery-workers`, TTL `DLUX_CELERY_HEALTH_TTL` default 30s, 0=disable) so repeated Options loads don't re-ping. Uses set/get on a specific key — NOT `cache.clear()` (sessions safe). (c) NEW optional COMPOSER VERSION stat in System Info tile (next to Python/Django) — `options_view` reads `os.getenv('COMPOSER_VERSION')`→`composer_version` ctx, `includes/options.html` guards `{% if composer_version %}`, new `composer_version` en+ar string. Mirrors existing `DECRYPTER_VERSION`→decrypter_version pattern (env auto-injected by the composer; env name confirmed by user = `COMPOSER_VERSION`). NOT YET browser/runtime-verified against live Redis/worker/composer env; py-compile OK.
  - [x] v1.2.14 RELEASED (all in CHANGELOG; 654 green): Options polish (landing ENTRY SELECTOR, CIRCULAR public-root theme swatch picker, reset danger strip) + OPTIONS RESTRUCTURE (pinned `.dlux-admin-panel-card` merging System Info/Backup/Settings) + fixes (admin update notification, API-dup discovery fix, per-user modal-size no longer clobbered by global default). NOTE settings-form choice fields use `DluxChoiceSelectorWidget` variants, not new components; forms.py changes need web reload.
  - [x] v1.2.13 RELEASED (tag `v1.2.13`, commit `98ddb52`) — see CHANGELOG for full detail. Shipped: appearance/anti-bot config toggles (layout `sticky_table_headers`/`zebra_striping`/`default_form_density`/`default_modal_size`; registration `honeypot_enabled`; public_root theme/title/meta + `show_titlebar_on_public`/`show_sidebar_on_public` superseding the legacy titlebar hide flag); per-user Form Density + Modal Size Options cards; sticky headers fixed (overflow wrappers trapped `position:sticky`; sticky-on→`overflow:visible`, `top:-1.5rem` flush under titlebar, solid `--table-row` backdrop); updater recovery hardening (reconcile reverts unreconstructable active→baked + self-heals stuck degraded; supervisor bakes from `dlux.__version__`); scan_link idempotency; and the `<title>`-in-help-text wizard-truncation fix (LESSON pinned above). All no-migration/inline-safe. Older released items v1.2.4–v1.2.12 (dlux_setup AST, footer, loading-button helper, updater UX, nginx envsubst, dlb-viewer, backup scope `0006`, etc.) — all in CHANGELOG.

### One-line info about last verified Tests:
- 2026-07-04: v1.3.0 full suite GREEN in throwaway venv: 667 tests (654 prior + 11 `test_groups.py` incl. 2 preset-inheritance widget/save + inline-safe guard + `test_activitylog.py` badge regression). Verified inherited perms render disabled+checked and are NOT written to direct user_permissions on save (stay group-only via has_perm). Added both new modules to `test_all.py` TEST_LABELS (CI runner uses an explicit list, not autodiscovery — else CI skips them); `python dlux/tests/test_all.py` now = 667. Migration 0007 is inline-safe (proven by `release_check.validate_inline_migrations('v1.2.14')`). New guard `ManifestTests.test_local_migrations_honor_manifest_inline_safe_claim` runs the REAL migrations through the validator whenever manifest.inline_safe=true (verified: fails on a probe AlterModelOptions, passes clean). Also a scripted end-to-end smoke (preset create→assign→edit-propagation→remove→group-centric add→visibility). `makemigrations --check` clean. Fixed 2 pre-existing asserts for the theme cache-buster bump. NOT browser-run (no live server in package repo).
- 2026-06-27: v1.2.13 full suite GREEN in throwaway venv (+playwright): 652 tests, all pass (config + updater-recovery + 2 new form/modal-density prefs tests). Browser-verified Options cards apply/persist/roundtrip + settings-modal live preview. Fixed two PRE-EXISTING failures (baseline v1.2.12 failed both): (1) `test_supervisor` now pins `DLUX_BAKED_VERSION='9.9.9-supervisor-test'` in the subprocess env + asserts the sentinel (dropped importlib.metadata derivation); (2) `test_user_profile_stats` full-suite ordering — `model_name='TestModel'` collided with `test_signals.TestModel` (registered process-wide → resolves to an ineligible dlux model → count 0); switched to unique `ProfileStatsFixtureModel` so report eligibility falls through to include. New tests: 6 in test_system_registry + 3 in test_defaults_and_urls; fixed titlebar-field assertions.
- 2026-06-24→26: earlier baselines — v1.2.11 full suite 631 OK; CI fixes: footer_enabled gated behind `_should_apply_db_override` (density-override clobber), `.env` line count, test_updater `setRootStatus` assertion.

### One-line info about last time edited Docs:
- 2026-07-03: v1.3.0 docs — admin-guide.md new "Permission Groups / Presets" section (manage_groups, presets, members/history, scope rules, models); reference.md new group routes + 5 preset helpers; FEATURES.md +Permission Groups feature/models. (Prior v1.2.13: layout/public_root/honeypot keys in reference/customization/admin/registration docs.)

## Part 2: Global
### Global Standard Helpers, Shortcuts, Info, etc.:
- Prefer `rg`/`rg --files` for discovery; inspect durable updater runs through the database, not web access logs alone.

### Global Rulesets:
- Keep tracker/changelog/docs synchronized with verified code and executed checks.

### Agent Handoff Rules:
- Read `tracker.md` every turn; preserve user work; use tag state plus release manifest before changelog/version edits.

### References and Links:
- Security: `docs/security-dsrp-1.md`; updater: `docs/inline-updater.md`; release: `docs/RELEASING.md`.
