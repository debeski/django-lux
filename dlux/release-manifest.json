{
  "schema_version": 1,
  "version": "1.5.3",
  "inline_safe": true,
  "minimum_updater_schema": 1,
  "migration_policy": "backward_compatible",
  "summary": "Adds a shared project-version helper that reads the version from a root release-manifest.json file.",
  "highlights": [
    "dlux.utils.get_project_version(BASE_DIR) reads and trims the version from the project's release-manifest.json.",
    "The helper can default to settings.BASE_DIR and returns an empty string for missing or malformed manifests.",
    "The existing get_app_version(__file__) VERSION-file helper remains unchanged."
  ],
  "release_url": "https://github.com/debeski/django-lux/releases/tag/v1.5.3"
}
