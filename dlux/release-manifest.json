{
  "schema_version": 2,
  "version": "1.9.0b1",
  "display": {
    "summary": "Release channels: stable by default, beta as an explicit opt-in. Plus a fix for dynamic-modal footer fields (is_active) that were never submitted.",
    "highlights": [
      "Stable and beta are separate channels. Stable is the default and excludes every prerelease; beta is an explicit opt-in in Options.",
      "Dynamic-modal footer fields such as is_active are submitted with the form again, so modal saves no longer store is_active=False.",
      "The Options beta switch works on a read-only runtime mount instead of returning a server error."
    ],
    "release_url": "https://github.com/debeski/django-lux/releases/tag/v1.9.0b1"
  },
  "requires": {
    "updater_schema": ">=1",
    "baked_image": ">=1.2.7",
    "services": {
      "composer": ">=1.3.14b1"
    },
    "migration_baseline": ">=1.8.14b1"
  },
  "migrations": {
    "effect": "additive",
    "rollback_compatible": true,
    "downtime": "none"
  },
  "install": {
    "inline": "allowed"
  },
  "rollback": {
    "supported": true
  }
}
