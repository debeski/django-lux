{
  "schema_version": 1,
  "version": "1.5.2",
  "inline_safe": true,
  "minimum_updater_schema": 1,
  "migration_policy": "backward_compatible",
  "summary": "Makes first-launch config.json bootstrap deterministic during migrator startup instead of depending on the first setup-page request.",
  "highlights": [
    "migrator applies a valid BASE_DIR/config.json immediately after database migrations and before web readiness.",
    "The setup view remains a row-locked fallback, while missing, invalid, and already-configured outcomes are logged explicitly.",
    "No schema change; existing configured systems continue to ignore config.json."
  ],
  "release_url": "https://github.com/debeski/django-lux/releases/tag/v1.5.2"
}
