{
  "schema_version": 2,
  "version": "1.9.1b1",
  "display": {
    "summary": "A Deployment operations card in Options: run Composer's deployment check from the browser instead of a shell on the host.",
    "highlights": [
      "Options can run Composer's deployment check and shows its findings, without a shell on the deployment host.",
      "DjangoLux requests named operations only — read-only in this release — and holds no Docker authority."
    ],
    "release_url": "https://github.com/debeski/django-lux/releases/tag/v1.9.1b1"
  },
  "requires": {
    "updater_schema": ">=1",
    "baked_image": ">=1.2.7",
    "services": {
      "composer": ">=1.4.1"
    },
    "migration_baseline": ">=1.8.14b1"
  },
  "migrations": {
    "effect": "additive",
    "rollback_compatible": true,
    "downtime": "none"
  },
  "install": {
    "inline": "allowed"
  },
  "rollback": {
    "supported": true
  }
}
