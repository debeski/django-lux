{
  "schema_version": 2,
  "version": "1.8.2",
  "display": {
    "summary": "The Options page defaults to tabs, Theme and Language are always separate cards, and the setup wizard scrolls to each step.",
    "highlights": [
      "Theme and Language are always their own Options cards; the size-dependent merge is removed.",
      "Options layout defaults to tabs; the style choices no longer share a constant with the default.",
      "Wizard Next/Back scrolls the new step into view rather than keeping the previous step's scroll position."
    ],
    "release_url": "https://github.com/debeski/django-lux/releases/tag/v1.8.2"
  },
  "requires": {
    "updater_schema": ">=1",
    "baked_image": ">=1.2.7",
    "services": {
      "composer": ">=5.3.0"
    }
  },
  "migrations": {
    "effect": "additive",
    "rollback_compatible": true,
    "downtime": "none"
  },
  "install": {
    "inline": "allowed"
  },
  "rollback": {
    "supported": true
  }
}
