{
  "schema_version": 2,
  "version": "1.9.2b1",
  "display": {
    "summary": "The Operations card can preview and apply Composer's deployment repairs, with the diff shown before anything is written.",
    "highlights": [
      "Preview what `composer check --fix` would change, as a diff, and apply it from Options after confirming your password.",
      "An apply is refused unless the deployment files still match the preview it was shown from."
    ],
    "release_url": "https://github.com/debeski/django-lux/releases/tag/v1.9.2b1"
  },
  "requires": {
    "updater_schema": ">=1",
    "baked_image": ">=1.2.7",
    "services": {
      "composer": ">=1.4.1"
    },
    "migration_baseline": ">=1.8.14b1"
  },
  "migrations": {
    "effect": "additive",
    "rollback_compatible": true,
    "downtime": "none"
  },
  "install": {
    "inline": "allowed"
  },
  "rollback": {
    "supported": true
  }
}
